sources:
  - name: jaffle_shop
    database: raw1-473601   #raw
    schema: dataset1        #jaffle_shop
    config:
          freshness:
            warn_after:
              count: 12
              period: hour
            error_after:
              count: 1
              period: day
          loaded_at_field: _etl_loaded_at
    tables:
      - name: customers
        freshness: null
      - name: orders